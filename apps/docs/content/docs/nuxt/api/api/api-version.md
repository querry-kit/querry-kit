# `ApiVersion`

```ts
type ApiVersion = 'v1';
```

The API path version accepted by [`createApiClient`](/docs/nuxt/api/api/create-api-client). The current public contract supports only `v1`, which produces an `/api/v1` base path.

```ts
import type { ApiVersion } from '@querry-kit/nuxt/api';
```
